/**
 * Auth Layout - login & register pages.
 * Centered card layout, no sidebar/header chrome.
 */
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
